-- 1. Find all sales greater than 1000
-- SELECT * FROM sales
-- WHERE volume > 1000;

-- 2. Find all recurring sales
-- SELECT * FROM sales
-- WHERE is_recurring IS TRUE;

-- 3. Find disputed sales with volume > 1000
-- SELECT * FROM sales
-- WHERE (is_disputed IS TRUE) AND (volume > 1000);